### custom color settings

# EVALUATION
# provide colors for each layer
# format: dict with key=layername; value=rgbvalue, both as strings
# for example:
# "nature": "0,128,0"
# if not provided, will generate colors automatically.